services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: incident-response-container
    ports:
      - "5000:5000"
    environment:
      TESTING_MODE: ${TESTING_MODE:-true}
    